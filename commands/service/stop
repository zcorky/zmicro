#!/bin/bash

help() {
  echo "Usage:"
  echo "  zmicro service stop <service_name> [options...]"
}

core() {
  if [ "$1" = "" ] || [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  local service_name=$1
  local args=${@:2}
  zmicro service down $service_name $args
}

run() {
  core $@
}

run $@
