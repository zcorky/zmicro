#!/bin/bash

help() {
  echo "Usage:"
  echo "  zmicro clean node_modules"
}

core() {
  clean::node_modules
}

run() {
  if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    help
    exit 0
  fi

  core $@
}

run $@
